// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/anzhiyu-c/anheyu-app/ent/articlepayment"
	"github.com/anzhiyu-c/anheyu-app/ent/predicate"
)

// ArticlePaymentQuery is the builder for querying ArticlePayment entities.
type ArticlePaymentQuery struct {
	config
	ctx        *QueryContext
	order      []articlepayment.OrderOption
	inters     []Interceptor
	predicates []predicate.ArticlePayment
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ArticlePaymentQuery builder.
func (apq *ArticlePaymentQuery) Where(ps ...predicate.ArticlePayment) *ArticlePaymentQuery {
	apq.predicates = append(apq.predicates, ps...)
	return apq
}

// Limit the number of records to be returned by this query.
func (apq *ArticlePaymentQuery) Limit(limit int) *ArticlePaymentQuery {
	apq.ctx.Limit = &limit
	return apq
}

// Offset to start from.
func (apq *ArticlePaymentQuery) Offset(offset int) *ArticlePaymentQuery {
	apq.ctx.Offset = &offset
	return apq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (apq *ArticlePaymentQuery) Unique(unique bool) *ArticlePaymentQuery {
	apq.ctx.Unique = &unique
	return apq
}

// Order specifies how the records should be ordered.
func (apq *ArticlePaymentQuery) Order(o ...articlepayment.OrderOption) *ArticlePaymentQuery {
	apq.order = append(apq.order, o...)
	return apq
}

// First returns the first ArticlePayment entity from the query.
// Returns a *NotFoundError when no ArticlePayment was found.
func (apq *ArticlePaymentQuery) First(ctx context.Context) (*ArticlePayment, error) {
	nodes, err := apq.Limit(1).All(setContextOp(ctx, apq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{articlepayment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (apq *ArticlePaymentQuery) FirstX(ctx context.Context) *ArticlePayment {
	node, err := apq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ArticlePayment ID from the query.
// Returns a *NotFoundError when no ArticlePayment ID was found.
func (apq *ArticlePaymentQuery) FirstID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = apq.Limit(1).IDs(setContextOp(ctx, apq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{articlepayment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (apq *ArticlePaymentQuery) FirstIDX(ctx context.Context) uint {
	id, err := apq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ArticlePayment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ArticlePayment entity is found.
// Returns a *NotFoundError when no ArticlePayment entities are found.
func (apq *ArticlePaymentQuery) Only(ctx context.Context) (*ArticlePayment, error) {
	nodes, err := apq.Limit(2).All(setContextOp(ctx, apq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{articlepayment.Label}
	default:
		return nil, &NotSingularError{articlepayment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (apq *ArticlePaymentQuery) OnlyX(ctx context.Context) *ArticlePayment {
	node, err := apq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ArticlePayment ID in the query.
// Returns a *NotSingularError when more than one ArticlePayment ID is found.
// Returns a *NotFoundError when no entities are found.
func (apq *ArticlePaymentQuery) OnlyID(ctx context.Context) (id uint, err error) {
	var ids []uint
	if ids, err = apq.Limit(2).IDs(setContextOp(ctx, apq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{articlepayment.Label}
	default:
		err = &NotSingularError{articlepayment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (apq *ArticlePaymentQuery) OnlyIDX(ctx context.Context) uint {
	id, err := apq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ArticlePayments.
func (apq *ArticlePaymentQuery) All(ctx context.Context) ([]*ArticlePayment, error) {
	ctx = setContextOp(ctx, apq.ctx, ent.OpQueryAll)
	if err := apq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ArticlePayment, *ArticlePaymentQuery]()
	return withInterceptors[[]*ArticlePayment](ctx, apq, qr, apq.inters)
}

// AllX is like All, but panics if an error occurs.
func (apq *ArticlePaymentQuery) AllX(ctx context.Context) []*ArticlePayment {
	nodes, err := apq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ArticlePayment IDs.
func (apq *ArticlePaymentQuery) IDs(ctx context.Context) (ids []uint, err error) {
	if apq.ctx.Unique == nil && apq.path != nil {
		apq.Unique(true)
	}
	ctx = setContextOp(ctx, apq.ctx, ent.OpQueryIDs)
	if err = apq.Select(articlepayment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (apq *ArticlePaymentQuery) IDsX(ctx context.Context) []uint {
	ids, err := apq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (apq *ArticlePaymentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, apq.ctx, ent.OpQueryCount)
	if err := apq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, apq, querierCount[*ArticlePaymentQuery](), apq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (apq *ArticlePaymentQuery) CountX(ctx context.Context) int {
	count, err := apq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (apq *ArticlePaymentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, apq.ctx, ent.OpQueryExist)
	switch _, err := apq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (apq *ArticlePaymentQuery) ExistX(ctx context.Context) bool {
	exist, err := apq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ArticlePaymentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (apq *ArticlePaymentQuery) Clone() *ArticlePaymentQuery {
	if apq == nil {
		return nil
	}
	return &ArticlePaymentQuery{
		config:     apq.config,
		ctx:        apq.ctx.Clone(),
		order:      append([]articlepayment.OrderOption{}, apq.order...),
		inters:     append([]Interceptor{}, apq.inters...),
		predicates: append([]predicate.ArticlePayment{}, apq.predicates...),
		// clone intermediate query.
		sql:       apq.sql.Clone(),
		path:      apq.path,
		modifiers: append([]func(*sql.Selector){}, apq.modifiers...),
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ArticleID uint `json:"article_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ArticlePayment.Query().
//		GroupBy(articlepayment.FieldArticleID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (apq *ArticlePaymentQuery) GroupBy(field string, fields ...string) *ArticlePaymentGroupBy {
	apq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ArticlePaymentGroupBy{build: apq}
	grbuild.flds = &apq.ctx.Fields
	grbuild.label = articlepayment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ArticleID uint `json:"article_id,omitempty"`
//	}
//
//	client.ArticlePayment.Query().
//		Select(articlepayment.FieldArticleID).
//		Scan(ctx, &v)
func (apq *ArticlePaymentQuery) Select(fields ...string) *ArticlePaymentSelect {
	apq.ctx.Fields = append(apq.ctx.Fields, fields...)
	sbuild := &ArticlePaymentSelect{ArticlePaymentQuery: apq}
	sbuild.label = articlepayment.Label
	sbuild.flds, sbuild.scan = &apq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ArticlePaymentSelect configured with the given aggregations.
func (apq *ArticlePaymentQuery) Aggregate(fns ...AggregateFunc) *ArticlePaymentSelect {
	return apq.Select().Aggregate(fns...)
}

func (apq *ArticlePaymentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range apq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, apq); err != nil {
				return err
			}
		}
	}
	for _, f := range apq.ctx.Fields {
		if !articlepayment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if apq.path != nil {
		prev, err := apq.path(ctx)
		if err != nil {
			return err
		}
		apq.sql = prev
	}
	return nil
}

func (apq *ArticlePaymentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ArticlePayment, error) {
	var (
		nodes = []*ArticlePayment{}
		_spec = apq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ArticlePayment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ArticlePayment{config: apq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(apq.modifiers) > 0 {
		_spec.Modifiers = apq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, apq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (apq *ArticlePaymentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := apq.querySpec()
	if len(apq.modifiers) > 0 {
		_spec.Modifiers = apq.modifiers
	}
	_spec.Node.Columns = apq.ctx.Fields
	if len(apq.ctx.Fields) > 0 {
		_spec.Unique = apq.ctx.Unique != nil && *apq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, apq.driver, _spec)
}

func (apq *ArticlePaymentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(articlepayment.Table, articlepayment.Columns, sqlgraph.NewFieldSpec(articlepayment.FieldID, field.TypeUint))
	_spec.From = apq.sql
	if unique := apq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if apq.path != nil {
		_spec.Unique = true
	}
	if fields := apq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, articlepayment.FieldID)
		for i := range fields {
			if fields[i] != articlepayment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := apq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := apq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := apq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := apq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (apq *ArticlePaymentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(apq.driver.Dialect())
	t1 := builder.Table(articlepayment.Table)
	columns := apq.ctx.Fields
	if len(columns) == 0 {
		columns = articlepayment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if apq.sql != nil {
		selector = apq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if apq.ctx.Unique != nil && *apq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range apq.modifiers {
		m(selector)
	}
	for _, p := range apq.predicates {
		p(selector)
	}
	for _, p := range apq.order {
		p(selector)
	}
	if offset := apq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := apq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (apq *ArticlePaymentQuery) Modify(modifiers ...func(s *sql.Selector)) *ArticlePaymentSelect {
	apq.modifiers = append(apq.modifiers, modifiers...)
	return apq.Select()
}

// ArticlePaymentGroupBy is the group-by builder for ArticlePayment entities.
type ArticlePaymentGroupBy struct {
	selector
	build *ArticlePaymentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (apgb *ArticlePaymentGroupBy) Aggregate(fns ...AggregateFunc) *ArticlePaymentGroupBy {
	apgb.fns = append(apgb.fns, fns...)
	return apgb
}

// Scan applies the selector query and scans the result into the given value.
func (apgb *ArticlePaymentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, apgb.build.ctx, ent.OpQueryGroupBy)
	if err := apgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArticlePaymentQuery, *ArticlePaymentGroupBy](ctx, apgb.build, apgb, apgb.build.inters, v)
}

func (apgb *ArticlePaymentGroupBy) sqlScan(ctx context.Context, root *ArticlePaymentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(apgb.fns))
	for _, fn := range apgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*apgb.flds)+len(apgb.fns))
		for _, f := range *apgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*apgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := apgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ArticlePaymentSelect is the builder for selecting fields of ArticlePayment entities.
type ArticlePaymentSelect struct {
	*ArticlePaymentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (aps *ArticlePaymentSelect) Aggregate(fns ...AggregateFunc) *ArticlePaymentSelect {
	aps.fns = append(aps.fns, fns...)
	return aps
}

// Scan applies the selector query and scans the result into the given value.
func (aps *ArticlePaymentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, aps.ctx, ent.OpQuerySelect)
	if err := aps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ArticlePaymentQuery, *ArticlePaymentSelect](ctx, aps.ArticlePaymentQuery, aps, aps.inters, v)
}

func (aps *ArticlePaymentSelect) sqlScan(ctx context.Context, root *ArticlePaymentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(aps.fns))
	for _, fn := range aps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*aps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aps *ArticlePaymentSelect) Modify(modifiers ...func(s *sql.Selector)) *ArticlePaymentSelect {
	aps.modifiers = append(aps.modifiers, modifiers...)
	return aps
}
